---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "semaphoreui_project_integration Data Source - semaphoreui"
subcategory: ""
description: |-
  The project integration data source allows you to read an existing webhook integration.
---

# semaphoreui_project_integration (Data Source)

The project integration data source allows you to read an existing webhook integration.

## Example Usage

```terraform
# Lookup by Integration ID
data "semaphoreui_project_integration" "by_id" {
  project_id = 1
  id         = 2
}

# Lookup by Integration Name
data "semaphoreui_project_integration" "by_name" {
  project_id = 1
  name       = "GitHub Webhook"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `project_id` (Number) The project ID that the integration belongs to.

### Optional

- `id` (Number) The integration ID. Either `id` or `name` must be specified.
- `name` (String) The display name of the integration. Either `id` or `name` must be specified.

### Read-Only

- `auth_header` (String) The custom header name for authentication (e.g., `X-Webhook-Token`). Used with `token` authentication method.
- `auth_method` (String) The authentication method for the integration webhook. Valid values are `token`, `github`, `bitbucket`, `hmac`, `basic`.
- `auth_secret_id` (Number) The ID of the project key containing the secret used for authentication.
- `searchable` (Boolean) When enabled, the integration uses matchers to route incoming webhooks via the project alias. When disabled, the integration has its own dedicated alias endpoint.
- `template_id` (Number) The template ID that this integration will trigger.
